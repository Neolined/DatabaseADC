Код создания таблицы products - create table `products1`(`UID` INTEGER(3) not null auto_increment,`type` VARCHAR(50) not null,  `name` VARCHAR(50) NOT NULL, `perfomance` VARCHAR(100), `serial` VARCHAR(100) not null unique, `enter` varchar(100), `date` date, `location` nvarchar(40),`owner` nvarchar(100), `software` varchar (50), `otk` nvarchar(40), `comment` nvarchar(255), primary key (UID))
Код внесения данных в тб products - insert into products2 values ('1','Плата', 'SW-01', 'FS-01', 'CO8199', 'CO8123', '2020.09.1', 'Склад', 'ООО АДС', 'r1930', 'Не проверялась', 'Лучше быть первым-последним, чем последним-первым')
Код создания products - CREATE TABLE products (`type` nvarchar(50) NOT NULL, `name` VARCHAR(30) NOT NULL,`serial` VARCHAR(30) NOT NULL UNIQUE,`date` DATE NOT NULL,`repairs` ENUM('yes','no') NOT NULL);
Код создания тб products1 - create table `history` (`UID` INTEGER(3) not null,`worker` nvarchar(40),  `date` datetime, `type_write` nvarchar(40), `order_from` nvarchar (30), `whom_order` nvarchar(30), `number_order` varchar(30), `empty_column` varchar(2), `repair_possition` varchar(10), `repair_element` varchar(10), `comment` nvarchar(255));
Код создания таблицы перечень изделий create table `list_of_products`(`type` nvarchar(50) not null, `name` varchar(50) not null, `developer` nvarchar (100) not null, `version_sp` tinyint not null);
Код занесения данных в уже существующую строку update users set root = 'accept' where user = 'samodel1';